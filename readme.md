# Heart Disease Predictor


## Backend:
```
python3 -m venv env
source env/bin/activate
pip install -r requirements.txt
(run the below two commands in separate terminals/CLI)
uvicorn app:app --reload --port 2006
streamlit run visual_app.py
```


## Frontend:
```
npm i
npm run dev
```